@model List<OficinaModel>
@{
    ViewData["Title"] = "Oficinas";
    var mensagemCadastro = TempData["MensagemCadastro"] as string;
}
@if (!string.IsNullOrEmpty(mensagemCadastro))
{
    <div class="alert alert-success">
        @mensagemCadastro
    </div>
}
<div class="text-center">

    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-outline-info" role="button" asp-controller="Oficina" asp-action="Create">Indicar nova Oficina </a>
    </div>

    <div>
        <h1 class="display-8">Todas as Oficinas:</h1>
    </div>
    <table class="table table-hover" style="height: 100px">
        <thead>
            <tr class="table-active">
                <th scope="col">Nome</th>
                <th scope="col">Estado</th>
                <th scope="col">Cidade</th>
                <th scope="col">Endereço</th>
            </tr>
        </thead>
        @if (Model != null && Model.Any())
        {
            foreach (OficinaModel oficina in Model)
            {
                <tbody style="overflow: auto;">
                    <tr >
                        <td scope="row">
                            <a role="link" asp-controller="Oficina" asp-action="Show" asp-route-id="@oficina.id">
                                @oficina.nome
                            </a>
                        </td>
                        <td>@oficina.estado</td>
                        <td>@oficina.cidade</td>
                        <td>@oficina.endereco</td>
                    </tr>
                </tbody>
            }
        }
        else
        {
            <h1>Não existem oficinas cadastradas na plataforma</h1>
        }
    </table>


</div>